$paths = Get-ChildItem c:\Users\ttintera\!Develop\RunBTEQ\
foreach ($p in $paths) {
    $r = $p.FullName -Replace([regex]::escape($RootSrcPath), "")
    $Split = $r.Split("\")
    $Split[0..($Split.count - 2)] -join "\"
}